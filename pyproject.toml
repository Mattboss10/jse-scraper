[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "jse-scraper"
version = "0.1.0"
dependencies = [
    "pandas",
    "playwright",
    "lxml",
    "beautifulsoup4"
]

[tool.setuptools.packages.find]
where = ["src"]